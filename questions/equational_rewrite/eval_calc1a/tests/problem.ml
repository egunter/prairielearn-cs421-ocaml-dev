(* File: problem.ml *)
(* Author: Elsa L. Gunter *)
(* Share and Enjoy *)

let problem_name = "eval_calc1a"

(* Gives the series of declarations that make up the parts of the question. *)
let problem_string = "Eval(if x > 2 then 3 else x + 3, {x -> 1})"
